fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.35
pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.2.0
python-dotenv>=1.0.1
# Postgres driver (production). Unused when DATABASE_URL points at SQLite.
psycopg2-binary>=2.9.9
# Test-only, kept here so `pip install -r requirements.txt` gives a runnable test suite.
httpx>=0.27.0
pytest>=8.3.0
